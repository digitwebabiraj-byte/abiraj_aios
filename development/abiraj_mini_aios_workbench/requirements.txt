# Python dependencies for every automated job in this workbench.
#
# Only two packages are actually imported by the runners; everything else they use
# (os, sys, json, hashlib, datetime, calendar) is in the standard library.
#
#   psycopg2  - the PostgreSQL driver. Every runner connects directly with it, never
#               through an MCP: a Scheduled Task has no MCP session.
#   openpyxl  - reads the approved .xlsx templates/mockups and writes the workbook
#               deliverables (T7, ZSFO, FRRC, ERA, EPPA).
#
# Install on a new machine:   pip install -r requirements.txt
# See NEW_MACHINE_SETUP.md for the full four-step restore.
#
# Versions below are the ones proven working on 2026-07-21 (Python 3.14.5).
# Pinned with >= rather than == so a newer patch release is allowed; if a future
# version ever breaks a runner, pin it exactly here and say why.

psycopg2>=2.9.12
openpyxl>=3.1.5
