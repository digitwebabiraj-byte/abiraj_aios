# Closure record — REQ-14-D01 / D02 (recorded 2026-07-27)

**Status: AMBER — DELIVERED · PUBLISHED · AUTOMATED · LIVE-VERIFIED, but not formally signed off.**
The eBay Return Analysis dashboard is built, published to four users and running on a monthly schedule,
and reconciles to the June-2026 reference to the penny. It is **not** recorded as a clean close because
(a) no reviewer/business sign-off is on file and (b) an unresolved source-of-truth paperwork conflict is
flagged (see Unresolved gaps). This record is a status closure, not a final sign-off.

## Scope checked
Read-only against the live `ledsone` DB (data) + the warehouse `tech_team_outputs.ph_task` (publish only).
No source table written, no DDL, no schema change. Reference period June 2026.

## What was delivered (REQ-14-D01)
- Per-SKU eBay Return Analysis dashboard — **HTML (~521 KB)** + matching **xlsx**, one row per variant SKU
  with ≥ 1 eBay return in the period; **19 columns** (SKU · Title · Account · Orders · Returns · Return Rate
  · LM/LY Returns · Refund £ · Return Cost £ · Main Reason · Rank · Neg Feedback · Open Cases · Stock ·
  Ad Spend/Sales · ACOS · ROAS) + Return-Reason Breakdown + Filter Options + Before/After panels.
- HTML renders **7 in-month date-range views** (Full · 1st/2nd half · Weeks 1–4) via pure-CSS toggles so
  they work in the no-JS ph_task viewer.

## Published
Per-user to `tech_team_outputs.ph_task`, `project_code=ERA`, `assigned_user_team=ebay_priors`, `released`:
| id | user | task_id |
|---|---|---|
| 387 | Thinesh | `ERA_Thinesh_ebay_return_analysis_2026-06` |
| 388 | Jarsini | `ERA_Jarsini_ebay_return_analysis_2026-06` |
| 389 | kobiga | `ERA_kobiga_ebay_return_analysis_2026-06` |
| 390 | powsteena | `ERA_powsteena_ebay_return_analysis_2026-06` |
Guarded `temp_user` publish: pre-flight read-only check, **pre-DELETE by task_id + plain INSERT** (no real
UNIQUE on `task_id`), `assigned_user_team` set (missing from the sample DDL), md5-verified, rolled back on
mismatch.

## Automation (REQ-14-D02)
Windows Task **`ERA_Monthly_Dashboard`**, **day 5 monthly @ 09:30** (next run 2026-08-05, reports July 2026),
`run_era_monthly.bat` → `automation/era_monthly_run.py`. Dynamic last-complete-month window; connects
`ledsone` (read) + warehouse (publish) via direct psycopg2 (no MCP session in a Scheduled Task). **Seven
pre-write gates** (zero-SKU, floor `ERA_MIN_SKUS`, `returns ≥ SKUs`, non-negative money, June-2026 anchor,
render/placeholder, **>40% collapse guard**), md5-verified publish, credentials via the global store,
`--dry-run`/`--no-publish`. Monitoring: `era_status.txt` + Desktop alert (`era_alert.ps1`).

## Validation result
**LIVE-VERIFIED 2026-07-20** — canonical SQL run against `ledsone` reproduces the reference exactly:
**144 SKUs / 153 returns / 17.7% / £2,937.37 refund / £869.39 return cost / £1,387.96 ad spend /
£9,343.63 ad sales / 14.9% ACOS / 6.73x ROAS**. Windows partition cleanly (h1 82 + h2 71 = 153; w1–w4
27+46+43+37 = 153). Forced floor and a forced 500→144 collapse both abort with nothing published.
Record: `validation/REQ-14_ebay-return-analysis/2026-07-20_live_count_verification.md`.

## Method (locked)
SKU via the `transaction_id` bridge (not `item_id`); Return Cost = REFUND + FINAL_VALUE_FEE; Advertising =
**CPC + CPS** combined (performance_data is CPC-only; CPS = AD_FEE/PREMIUM_AD_FEES per-sale); text-typed
numerics cast; earliest-row reason/refund vs newest-row state; intentional blanks preserved.

## Duplicate risks
None. First per-SKU eBay returns report in the portfolio.

## Unresolved gaps (why this is AMBER, not closed)
1. **Source-of-truth paperwork conflict (STOP condition).** The project `CLAUDE.md` and the TASK_REGISTER
   "One next action" still describe `REQ-14`/`ERA` as *working defaults, owner confirmation pending* and
   *"do not publish without explicit owner instruction / D01 not yet run"* — while a scheduled task
   publishes monthly, unattended, as `released`. The automation is very likely the correct state and the
   paperwork is stale, **but that must be reconciled by the owner, not assumed.**
2. **No reviewer/business sign-off recorded** (Sajeesan technical, Tamil Selvan queryability).
3. Full 144-row diff + workbook rebuild vs the reference still pending.

## Decision
**AMBER — delivered, published (4 users), automated and live-verified; closure blocked on owner
reconciliation of the identifier/publish paperwork and recorded reviewer sign-offs.**

## One next action
Owner to reconcile the stale `CLAUDE.md`/"One next action" wording against the live `released` automation
and confirm the `REQ-14`/`ERA` identifiers; then record the reviewer gates and upgrade this record to a
full close.

## Git
`539bf1b`, `d29bff5`, `04b6ed0`.
