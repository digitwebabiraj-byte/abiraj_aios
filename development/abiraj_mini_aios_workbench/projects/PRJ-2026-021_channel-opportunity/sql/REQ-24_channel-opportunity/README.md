# sql / REQ-24 — Channel Opportunity build code

Single read-only builder lives here (planned: `chop_build_d01.py`) plus any dashboard renderer + fonts.
One fetch path only (the one-generator-module rule). No secrets in git. Empty until the build starts —
blocked on discovery with Mahima (see `../../PROJECT_HOME.md`).
