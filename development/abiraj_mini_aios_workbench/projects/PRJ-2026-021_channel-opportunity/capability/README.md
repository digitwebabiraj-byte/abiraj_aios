# Capability — reusable extracted methods

Reusable toolkits/methods distilled from this project (e.g. the cross-channel base-SKU sales pivot, the
Opportunity classifier — Shopify winner / Marketplace winner / Missing channel — and the Action rule
engine). Empty until a method is promoted for reuse.
