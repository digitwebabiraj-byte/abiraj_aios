# automation — REQ-24 Channel Opportunity

Scheduled-refresh code (fail-closed runner, secrets template, register script) if/when the report is
automated after sign-off — following the fmp pattern (`FMP_Weekly_Fast_Moving_Products`). Empty until the
report is built, delivered and Mahima requests a recurring refresh. No secrets in git.
