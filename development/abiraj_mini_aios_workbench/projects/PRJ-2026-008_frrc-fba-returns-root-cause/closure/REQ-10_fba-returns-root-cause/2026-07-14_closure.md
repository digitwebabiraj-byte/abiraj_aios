# Closure — REQ-10_fba-returns-root-cause (D01)

**Date:** 2026-07-14 · **Owner/Dev:** Abiraj · **Status:** COMPLETE (technical) — PUBLISHED per-PH
(rows 216–234); VALIDATION GREEN; **REVIEWER (Tamil Selvan / Sajeesan) + BUSINESS SIGN-OFF
(Satheesvaran, A–G) PENDING**.

## Scope checked
Read-only reporting + governed per-PH publish, in a new project folder
`projects/PRJ-2026-008_frrc-fba-returns-root-cause/`. Production DB inspected read-only via the Postgres
MCP; **no source table created/altered** (`amazon_returns` / `order_transaction` / `listing_data` all
read-only); the only writes were the guarded per-PH INSERT/UPDATE into the output store
`tech_team_outputs.ph_task`. Nothing outside the project folder written except the workbench
`PROJECT_REGISTER.md` row; committed + pushed to `main` on instruction.

## Assets inspected
Workbench governance docs; PRJ-2026-006 / PRJ-2026-007 as the structural template; the
`postgres-warehouse-sql` skill; the FRRC handoff bundle (`HANDOFF_FRRC_REQ-10-D01.md`, the requirement
doc, execution prompt, `frrc30.json`, `build_*.py`) from `Downloads/files (5).zip`; the live
`tech_team_outputs.ph_task` schema + existing per-PH projects (PSLD/WLSP/ph-asin) for the routing
pattern.

## Files created
- Project root: `README.md`, `PROJECT_HOME.md`, `SYSTEM_REFERENCE.md`, `CLAUDE.md`, `TASK_REGISTER.md`.
- SQL: `sql/REQ-10_.../generate_report.sql`, `reason_domain_check.sql`, `validation_checks.sql`.
- Source docs (COPY-only + SHA-256): `evidence/source_documents/REQ-10_.../HANDOFF_FRRC_REQ-10-D01.md`,
  `2026-07-14_abiraj_REQ-frrc_REQ-10-D01.md`, `FRRC_REQ-10-D01_execution_prompt.md`, `SOURCE_MANIFEST.md`.
- Outputs: `evidence/final_outputs/REQ-10_.../` — `frrc30.json` (system of record), `build_frrc30.py`,
  `build_console.py`, `build_per_ph.py`, `FRRC_FBA_Returns_Console_REQ-10-D01_30day.html`,
  `per_ph/<PH>.html` ×19 + `per_ph/_manifest.json`.
- Evidence: `evidence/logs_or_screenshots/REQ-10_.../2026-07-14_import_checksum_evidence.md`,
  `2026-07-14_per_ph_publish_record.md`.
- Validation: `validation/REQ-10_.../2026-07-14_validation.md`.
- Closure: this file.

## Files modified
- `PROJECT_REGISTER.md` (workbench root) — added the PRJ-2026-008 row.

## Evidence created
Independent dataset re-verification (91 ASINs / 105 returns / 0 bucket-arithmetic failures); live
`ph_task` schema pull (caught the missing `assigned_user_team` column); PH-roster cross-check (19/19
exact vs the canonical `ph_priors` roster); the governed pull `frrc30.json`; all-owners console parity
diff (91/91 tuples); 19 per-PH dashboards; guarded per-PH publish (ids 216–234) with per-row md5
verification pre-commit and independent read-only MCP re-verification. md5s recorded in `_manifest.json`
and the publish record.

## Duplicate risks
GREEN — first FBA-returns / root-cause asset in the workbench; new sequential Project ID and
source-derived Task ID; `project_code='frrc'` confirmed unused before publish; no canonical asset
overwritten; source docs COPY-only (originals preserved).

## Unresolved gaps
Business decisions **A–G** for Satheesvaran (order-status set · marketplace scope · window length ·
returns↔sales alignment · rare reason codes · **return-status filter — the only one that changes the
numbers** · attribute the 18 unassigned owners). Reviewer gates: Tamil Selvan (queryability), Sajeesan
(technical). Remaining outputs to import: the `.xlsx` tracker + the simpler grouped `.html` (regenerable
from `frrc30.json`). No recurring schedule wired (query is `CURRENT_DATE`-ready → would be REQ-10-D02).

## Validation result
GREEN (technical) — control totals reconciled, cross-checks pass, outputs parity-exact, publish
md5-verified live. Business + reviewer sign-off pending.

## Decision
**GREEN (technical).**

## Addendum (2026-07-14) — per-PH publish (rows 216–234)
Published one row per named holder to `tech_team_outputs.ph_task` (guarded `temp_user` write, INSERT-only,
one txn, md5-verified pre-commit; `project_code=frrc`, `assigned_user_team=ph_priors`, `version_status=released`,
`task_id=frrc_<PH-slug>_fba_returns_root_cause-V1`). 19 rows / 19 holders, covering 73/91 ASINs; the 18
unassigned N/A ASINs route to nobody (open item G). Identifiers from the requirement doc. Re-verified via
read-only MCP (19 rows / 19 users / all `ph_priors` / all `released`). Publish scripts kept in the
scratchpad (credential — not committed).

## Addendum (2026-07-14) — UI iterations V2→V5 (portal fit)
On owner feedback the per-PH dashboard was iterated in place (guarded md5-verified UPDATEs, identity
unchanged): **V2** document-flow layout (fixed the cramped `100vh` inner-scroll box); **V3** full-width
data table; **V4** self-describing HTML header + shortened `task_name` and `description=NULL` to shrink
the portal's top band; **V5** root-caused and fixed the sticky column-header ghost (`.wrap{overflow-x:auto}`
was trapping `position:sticky`) — header now pins at `top:0`, table fits the portal width with no
page horizontal scroll (verified in-browser at 1360px and 1120px). Live `version_level` now **5**.

## One next action
Route open items **A–G to Satheesvaran** — start with **F (return-status filter)**, the only one that
changes the numbers; then **C (window length)** and **E (rare reason codes)**. In parallel submit for
Tamil Selvan (queryability) + Sajeesan (technical) sign-off. On sign-off, close D01 and, if a cadence is
wanted, open **REQ-10-D02** (scheduled run, `CURRENT_DATE`-based). Do not change any locked rule without
Satheesvaran sign-off.
