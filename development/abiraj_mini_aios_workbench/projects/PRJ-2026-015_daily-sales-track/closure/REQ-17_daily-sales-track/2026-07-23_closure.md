# Closure record — REQ-17-D01 / D02 (2026-07-23)

**FULL CLOSURE — REQ-17 CLOSED 2026-07-23.** Daily Sales Track delivered, published to
`tech_team_outputs.ph_task` (ids 422–425, v9, md5 `642a5a27`), automated daily at 09:05, and signed
off by Thinesh (business), Sajeesan (technical), Tamil Selvan (queryability) and Varmen
(coordination/IDs). No open items.

## Scope checked
Read-only against live `ledsone` for source data + `tech_team_outputs.ph_task` (publish target only).
The only approved write was the guarded, SELECT-then-UPDATE publish to the four `dst` rows in place.
No source table written, no DDL, no schema change.

## Method (locked)
30 rows at **account × marketplace**, 24 columns, daily grain over the whole eBay channel, audience
`ebay_priors`. **Currency trap enforced:** `orders.total` is in each marketplace's own currency and is
**never blended into GBP** (no FX table exists) — money is reported per currency. Definitions inherited
from REQ-13 (EBPD). Seller Hub anchor £837.93.

## Validation result
**GREEN.** 18/18 verification checks; delivered state = 30 rows, money per currency, live on ph_task
422–425 (v9, md5 `642a5a27`); daily 09:05 job registered and proven end to end. `project_code='dst'`
rows are clean (exactly 4, 4 distinct task_id, 1 HTML version, 0 missing `assigned_user_team`).
Evidence: `validation/REQ-17_/2026-07-23_duplicate_check_and_signoff.md` +
`2026-07-23_workbook_verification.md`.

## Automation
Daily **09:05** Windows scheduled job rebuilds from live `ledsone` and updates the four ph_task rows
in place (stable `task_id`, so no accumulation). Credentials from the shared global store.

## Duplicate risks
None. Adjacent reports checked one by one — UDESC (weekly, per-Item-ID, `ph_priors`), espd/ebpd
(monthly), ebsr (stock not sales), EBAYAHD (mislabelled Amazon FBA), UAWSO (Amazon UK). Nothing else
reports daily eBay sales at account × marketplace to `ebay_priors`.

## Decisions — RESOLVED & SIGNED OFF 2026-07-23
Grain (account × marketplace, daily) · per-currency money, no blend (decision — no FX table) ·
date-free `task_id` for in-place daily update (decision I) · REQ-13 definitions inherited. Gates:
Sajeesan (technical), Tamil Selvan (queryability), Thinesh (business), Varmen (coordination) all
**signed off 2026-07-23**.

## Two disclosed, not-blocking items (carried, not open)
- `Active Listing` understated ~5–6% from stale `is_ended` flags on auto-renewing listings — disclosed
  on both artefacts; fix belongs to the listings sync, not this report.
- ±5% trend band is provisional (decision E) — editable config on the Config sheet.

## Registry-level findings raised (outside this project)
`ph_task` has no UNIQUE on `task_id` despite the sample DDL (30 duplicate rows, incl. 7 NULL-task_id)
and the OneDrive `0xC000013A` scheduler no-run trap — both routed to Sajeesan / `NEW_MACHINE_SETUP.md`.

## Decision
**GREEN — DELIVERED · PUBLISHED (ph_task 422–425) · AUTOMATED (daily 09:05) · SIGNED OFF — CLOSED
2026-07-23.**

## One next action
**None — REQ-17 CLOSED and self-running.**
