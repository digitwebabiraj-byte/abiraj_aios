# Capability — reusable extracted methods

Reusable toolkits/methods distilled from this project (e.g. the per-listing warehouse pull, the
account-brand map, the currency-per-marketplace formatter). Empty until a method is promoted for reuse.
