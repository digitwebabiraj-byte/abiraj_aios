# Closure record — REQ-16-D01 (recorded 2026-07-27)

**Status: AMBER — DELIVERED · VERIFIED · PUBLISHED · BUSINESS GATE CLOSED (Thinesh); technical reviewer
gates + three decisions open; not automated.** This is a status closure, not a final sign-off.

## Scope checked
Read-only across **two databases** — `ledsone` (the listing/sales/stock universe) + the warehouse
`public.traffic_data WHERE which_channel = 2` (eBay organic Views/CVR). All active eBay accounts,
**UK + Germany only** → **12 accounts · 16 account × marketplace · 11,156 listings** (UK 7,685 / DE 3,471;
`neighbourmarket` falls out, US-only). The single write is the authorised `ph_task` publish.

## What was delivered (REQ-16-D01)
One dataset rendered three ways (one deliverable):
- **(a) HTML dashboard** (`REQ-16-D01_esnm_dashboard.html`, ~3.2 MB) — reviewer console, **20 columns**,
  product thumbnails w/ hover-zoom + click-through, date/dimension filters, KPI cards that recompute on the
  filtered view, frozen Image/Account columns, row virtualisation for 11,156 rows.
- **(b) xlsx workbook** — 5 sheets, **21 columns** (the 20 + the split last-year comparator), live-formula
  engine driven by an editable Rules sheet.
- **(c) governed JSON** (`esnm_d01_data.json`) — the dataset both renderers read.
- 12-rule action engine with editable thresholds. Anchor 2026-07-22: **8,067 Critical (End Listing)** ·
  1,210 Clearance · 851 Price Cut · 476 SEO · 149 Bundle · 42 Competitor Review · 26 Listing Quality ·
  2 Pause PPC · 109 Maintain · 53 Grow · 171 Monitor.

## Published
`tech_team_outputs.ph_task` ids **411–414**, audience `ebay_priors` (Thinesh · Jarsini · kobiga · powsteena),
one row each (currently v7). Publisher `automation/publish_esnm_ph_task.py` — dry-run by default,
SELECT-then-UPDATE (no UNIQUE on `task_id`), sets `assigned_user_team` (omitted from the sample DDL).

## Validation result
**All 10 PASS/FAIL rules green** (P1–P10). In-sheet engine implemented **twice independently** (Excel +
Python) and diffed **11,156/11,156 identical, 0 mismatches**; **0 formula errors** workbook-wide; item
`164889807930` reconciled exactly on 7 fields. Independent harness (`validation/REQ-16_.../verify_esnm_d01.py`,
does not import the builder) = **17/17 data + 9/9 UI checks, 0 failures**. Fixed during build: KPI cards not
recomputing on filter; a sort-map pointing at non-existent columns 19/20; embedded-iframe layout defects.

## Business validation
**Dashboard verified by Thinesh ("all ok") 2026-07-23** — business gate closed. **Decisions C (rule
precedence — Critical→High→Medium→Low, first match wins) and F (actionability — 8,065 Critical rows stand,
no ranking/cap) CLOSED by Thinesh 2026-07-23.** Rule 10 accepted as permanently shadowed/dead.

## Key structural findings (disclosed inside the deliverable)
- **Needs two databases** — a `ledsone`-only build misses Views/CVR; a warehouse-only build has Product
  Title on only 8.3% of items.
- **`Watchers` (col 17) UNAVAILABLE** — exists in neither DB (eBay Trading-API only, not ingested).
  **Rule 6 can never fire.** Column ships blank on the workbook, removed from the dashboard (owner
  instruction 2026-07-22) → the 20-vs-21 column divergence.
- **Rule 10 unreachable** — any "age>180d AND last sale>90d" listing has zero 90-day sales, so Rule 1
  claims it first (0 of 11,156).
- **51.7% of listings carry `wrong_sku=1`** (5,767) — real sellable listings; deliberately **NOT** filtered
  (excluding would delete half a dead-stock report). Disclosed.
- **11 eBay traffic days lost** in the 90-day window (eBay-specific, not a pipeline outage); rules 5/9
  evaluated only where traffic rows exist, absent traffic rendered blank not zero.

## Duplicate risks
None. First slow/no-mover report in the portfolio.

## Unresolved gaps (why this is AMBER, not closed)
1. **No technical/queryability/coordinator sign-off recorded** (Sajeesan, Tamil Selvan, Varmen).
2. **Decision H OPEN — partial-day anchor.** Rebuilds drift (Rule 1 8,067→8,066; row count 11,156→11,176);
   not yet fixed because it changes already-published figures.
3. **Decision G OPEN** — Rule 8 "PPC Spend High" threshold (£5.00/30d, an implemented assumption).
4. **Decision B OPEN** — the 11 lost traffic days (likely recoverable by re-running the eBay Analytics pull).
5. **Not automated** — REQ-16-D02 not started (blocked on decision D, cadence; would be the fleet's 7th job).
6. ID confirmation (`REQ-16`/`esnm`) still marked pending.

## Decision
**AMBER — delivered, verified, published and business-signed-off (Thinesh); technical reviewer gates and
decisions B/G/H open; not automated.**

## One next action
Fix decision **H** (pin the anchor to a complete day so rebuilds stop drifting) and obtain the technical
reviewer sign-offs; then decide the D02 cadence.
