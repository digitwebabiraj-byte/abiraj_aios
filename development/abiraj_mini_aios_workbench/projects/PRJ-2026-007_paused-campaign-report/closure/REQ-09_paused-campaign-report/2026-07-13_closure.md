# Closure — REQ-09_paused-campaign-report (D01)

**Date:** 2026-07-13 · **Owner/Dev:** Abiraj · **Status:** COMPLETE (technical) — VALIDATION GREEN;
**BUSINESS SIGN-OFF (Satheesvaran) PENDING**.

## Scope checked
Read-only reporting task in a new project folder `projects/PRJ-2026-007_paused-campaign-report/`.
Production DB inspected read-only via the Postgres MCP; no DB object created/altered; nothing outside
the project folder written; not committed/pushed.

## Assets inspected
Workbench governance docs; PRJ-2026-006 as the structural template; the `postgres-warehouse-sql`
skill + `TABLE_ppc.md`; the handoff brief + `Utharsika_task.xlsx` from Downloads.

## Files created
- Project root: `README.md`, `PROJECT_HOME.md`, `SYSTEM_REFERENCE.md`, `CLAUDE.md`, `TASK_REGISTER.md`.
- SQL: `sql/REQ-09_.../generate_report.sql`, `sql/REQ-09_.../validation_checks.sql`.
- Source docs (COPY-only): `evidence/source_documents/REQ-09_.../CLAUDE_CODE_HANDOFF_Paused_Campaign.md`,
  `Utharsika_task.xlsx`, `SOURCE_MANIFEST.md`.
- Outputs: `evidence/final_outputs/REQ-09_.../` — `data.json`, `build_report.py`, `build_html.py`,
  `Paused_Campaign_Report_Utharsika.xlsx`, `Utharsika_Paused_Campaigns_Report.html` (**published
  dashboard**, owner-supplied), `PAUSED_CAMPAIGN_VERIFICATION_PACK.md`.
- Validation: `validation/REQ-09_.../2026-07-13_validation.md`.
- Closure: this file.

## Files modified
- `PROJECT_REGISTER.md` (workbench root) — added the PRJ-2026-007 row.

## Evidence created
Live SQL results (report + 2 validation queries), `data.json` governed pull (33 rows + metadata),
xlsx (3 sheets), interactive dashboard (rendered + verified over HTTP), independent 4-check
verification pack. md5s recorded in the build step and `SOURCE_MANIFEST.md`.

## Duplicate risks
GREEN — first PPC/paused-campaign asset in the workbench; new sequential Project ID and source-derived
Task ID; no canonical asset overwritten.

## Unresolved gaps
Business decisions A–E for Satheesvaran (scope key · grain · included set · platform · manual pauses);
B (grain) and C (included set) would change the row count. No recurring schedule wired (query is
already `CURRENT_DATE`-based → would be REQ-09-D02).

## Validation result
GREEN (technical) — 4/4 checks PASS, outputs reconciled. Business sign-off pending.

## Decision
**GREEN (technical).**

## Addendum (2026-07-13) — published dashboard adopted
The owner supplied a hand-finished dashboard (`Utharsika_Paused_Campaigns_Report.html`, md5
`6b971cf330e04404417db70ee6d70336`). Its embedded payload was diffed against `data.json`:
**33/33 row tuples match exactly, 32 ASINs, identical rule/wave counts, 0 differences.** Adopted as
the **canonical published dashboard**; my earlier auto-built `…_Dashboard.html` was **removed** to
avoid parallel truth; `build_html.py` retargeted to a secondary audit `…_dataview.html`. One
documented delta: the dashboard shows a **cleaned presentation** of the pause reason (drops the
`Date Range …` prefix, `≥`→`>=`, adds summary/chips) — verbatim reason retained in `data.json` + xlsx
(SYSTEM_REFERENCE §7; CLAUDE.md rule relaxed accordingly). Minor note: the dashboard pulls IBM Plex
from Google Fonts (CDN) and degrades to `system-ui` offline; report data is fully embedded.

## Addendum (2026-07-13 14:04) — published (row 215) + V2 table-hero redesign
Published the dashboard as a **new row (id 215)** in `tech_team_outputs.ph_task` (guarded single-row
INSERT as `temp_user`; `project_code=PC`, `task_id=PC_utharsika_paused_campaigns_dashboard-V1`,
`assigned_user=utharsika`, released; ZSFO precedent). On owner feedback that the table (the important
part) was cramped in the ops viewer, **rebuilt the dashboard as a table-hero, full-width layout** —
slim one-line KPI ribbon so the table fills the height; rule-coded left bands + badges (R1 ACOS red /
R2 zero-orders amber / R3 spend violet); heat-scaled Days Paused; system fonts (CDN dependency
removed). Same 33-row payload reused verbatim (data parity unchanged). Pushed as a guarded
`UPDATE … WHERE id=215 AND task_id=…` (version_level 1→2); live `html_content` verified
`md5=fbd4b6007110a6b440f0757111de1158` (34,213 chars), identical to the LF-normalised canonical file;
identity fields unchanged. Publish/update scripts kept in the scratchpad (not committed — credential).

## One next action
Route open items **A–E to Satheesvaran** for a business decision — primarily **grain** (per-ASIN vs
per-campaign) and **included set** (still-paused 33 vs all-pauses 41). On sign-off, close D01 and, if a
cadence is wanted, open **REQ-09-D02** (scheduled run). Do not commit/push or change any locked rule
without instruction.
