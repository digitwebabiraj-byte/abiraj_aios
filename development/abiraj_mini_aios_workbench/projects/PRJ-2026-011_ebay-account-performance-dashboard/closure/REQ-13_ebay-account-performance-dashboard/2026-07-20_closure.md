# REQ-13-D01 — Closure record (2026-07-20)

## Status: CLOSED / ACCEPTED
REQ-13-D01 (eBay Account Performance Dashboard, June 2026) is **delivered, published, and accepted by the
business owner Thinesh ("all ok", 2026-07-20)**.

## What was delivered
- Interactive **HTML** dashboard + matching 3-sheet **Excel**, both on the final method: rows = account ×
  marketplace (22 rows), Revenue = `SUM(order_total)` Completed, whole-account conversion
  (`which_channel=2`), Advertising = eBay Promoted Listings **ON_SITE (Priority) only** shown as **TACOS** +
  Return + Ad Sales, New Listings from the ledsone DB, Sales Rank by revenue.
- **Published live** per-user to `tech_team_outputs.ph_task` — ids **333 (Thinesh), 334 (Jarsini), 335
  (kobiga), 336 (powsteena)**, `project_code=ebpd`, `assigned_user_team=ebay_priors`, `released`.

## Reconciliation
Every headline reconciled to the owner's own live-DB checks (led_sone UK £28,975.37; so_926407 UK ON_SITE
ad £884.07). Excel recalc'd 0 errors; both formats consistent (Rev £95,455.18, ad £7,788.75, TACOS 8.16%).

## Resolved-as-accepted (owner defaults)
- Orders = `COUNT(DISTINCT order_id)` (line-count 1,619 noted, not adopted).
- Conversion RAG threshold kept as mockup's (flagged for recalibration; whole-account conversion ~2–3%).

## Not formally recorded
- Reviewer gates: Sajeesan (technical), Tamil Selvan (queryability) — not signed here.

## Reusable knowledge extracted (see the skill file)
`order_total` = the revenue basis · account × marketplace grain · `which_channel=2` = eBay traffic · listing
`created_at` lives in the ledsone DB · attributed eBay ad sales over-count → TACOS · ON_SITE via
`ppc.record_subtype` (join `record_id=parent_id`) · `ph_task` publish traps (no real `UNIQUE(task_id)`,
hidden `assigned_user_team`).

## Optional future — REQ-13-D02
- Scheduled monthly refresh (dynamic reporting month).
- Housekeeping: move the `temp_user` DB password out of `push_ebpd_dashboard.py` plaintext into a secret/env var.
