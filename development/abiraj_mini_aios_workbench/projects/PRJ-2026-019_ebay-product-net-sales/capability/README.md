# Capability — reusable extracted methods

Reusable toolkits/methods distilled from this project (e.g. the eBay per-order deduction pull, the
Final-Value-Fee attribution, the currency-per-marketplace formatter). Empty until a method is promoted
for reuse.
