# Evidence — logs & audits · REQ-22 eBay Product Net Sales

## What will land here
The read-only data-availability sweep behind every source claim in `SYSTEM_REFERENCE.md`, plus the
worked-example reconciliation transcript.

## Still to do (build not started)
- Live column-by-column availability sweep against `ledsone` (source_id=2, all_list=1), with coverage %.
- Confirm Final Value Fee / Promotion / PPC homes and the `General` bucket contents.
- Confirm the Product Cost verdict (expected: NO DATA — `sku_cogs` empty).
- Reconcile order `02-14934-76138` to the stated Net Sales of 22.39.
- Connectivity note (ledsone MCP vs direct).

**EMPTY until the discovery sweep runs.**
