# Capability — reusable extracted methods

Reusable toolkits/methods distilled from this project (e.g. the per-channel fast-mover ranking, the
Stock Cover Days calc, the cross-channel base-SKU roll-up, the Trend/Action rule engine). Empty until a
method is promoted for reuse.
