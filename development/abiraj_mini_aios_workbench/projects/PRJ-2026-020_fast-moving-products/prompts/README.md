# prompts — REQ-23 Fast Moving Products

GPT-brain prompts that drove each phase (discovery / implementation / validation / closure), stored per
phase subfolder. Claude Code executes these prompts; it does not author the plan.
