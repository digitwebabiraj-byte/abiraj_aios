# Closure record — REQ-15-D01 / D02 (recorded 2026-07-27)

**Status: AMBER — D01 DELIVERED · LIVE-VERIFIED · BUSINESS-VERIFIED (Meshika); D02 automation BUILT.**
Not a clean close: full technical/queryability reviewer sign-off is not on file, and the D02 scheduled-task
registration state needs owner confirmation (see Unresolved gaps). This is a status closure.

## Scope checked
Read-only against the **raw `ledsone` DB** (the confirmed source — the warehouse hides SMART campaigns) +
the AIOS knowledge base. The report only **recommends**; pausing in eBay Seller Hub stays manual. No write
to eBay. No `ph_task` publish is part of D01 (see the disclosed verification publish under D02).

## What was delivered (REQ-15-D01)
Read-only pause-recommendation report for **LEDSone eBay UK** — HTML dashboard + **Pause Log** with staff
approve/reject + xlsx, ON_SITE campaigns. Anchor 2026-07-20 (last complete day):
- **45 campaigns**, **15 recommended pauses** (8 Stock / 7 Rule 1 / 0 Rule 2), **£1,403.54 of £3,532.41**
  30-day spend at risk; 16 running, 14 off.

## Validation result
**LIVE-VERIFIED 2026-07-21.** The rule engine was independently re-implemented in SQL and diffed against
the shipped artefacts: **45/45 campaigns identical on all 6 fields (270 comparisons, 0 mismatches)**, all
10 KPIs reconciled, scope audit balanced (45 in scope · 73 CPS · 1 OFF_SITE · 145 deleted excluded),
HTML + xlsx matched the governed JSON, warehouse corroborated the campaign census. Record:
`validation/REQ-15_ebay-ppc-product-pause-automation/2026-07-21_live_data_verification.md`.
- **Defect fixed same day — partial-day anchor:** window had anchored on `MAX(date)` (= today, 8 clicks/
  £1.39); now resolves to the latest **complete** day. Decisions unchanged; money corrected
  £1,355.02 → £1,403.54 at risk and £3,400.40 → £3,532.41 total (exactly the swapped day).
- **Near-miss caught:** a PERF re-pull changed the row layout 15 → 12 fields; renderers had rebuilt from
  **stale** JSON. A `len(fields)` assertion now guards the parser.

## Business validation
**Business Validator = Meshika** (`staff.users` id 182, verified live). **Dashboard verified by Meshika
2026-07-21.** **Decision C CLOSED 2026-07-21 — Option A** (a listing is out of stock only when *every*
version is at zero; "any version zero" would have flagged 31/31 live campaigns).

## Automation (REQ-15-D02)
Autonomous weekly refresh **`EPPA_Weekly_Pause_Report`**, **Monday 11:00** (clear of the fleet), rolling
last-30-days anchored on the latest **complete** day, `ledsone` read-only via direct psycopg2. One shared
rule module `eppa_engine.py` imported by both the weekly run and any manual rebuild (they cannot drift).
**Fail-closed gates:** pull error · 0 campaigns · < 20 campaigns · > 40% collapse vs last good run · £0
total spend → previous report survives, `eppa_status.json` records why, `eppa_alert.ps1` drops a Desktop
alert. Credentials via the **global store** (a local `eppa_secrets.bat` still overrides but is no longer
required); `--dry-run`/`--no-publish` added.
- **Disclosed live publish during verification:** because no dry-run existed at the time, confirming the
  credential migration required a real run — it refreshed `ph_task` **id 405 → 407 (version 3)**; content
  identical (same anchor, 45 campaigns, 15 pauses, £1,403.54), a version bump not a data change, row count
  stayed at 1. The dry-run flag now makes this unnecessary.

## Fleet contribution
EPPA's **collapse guard** (reject a > 40% count fall vs the last good run, not just an absolute floor) was
**backported to T7, EPC, EBPD and ERA**. Correctly does **not** assert `version_status` after publish.

## Data traps recorded (AUTOMATION_README.md)
Warehouse hides SMART campaigns → use the raw `ledsone` DB; CPS campaigns log £0 spend (Rule 1 uncomputable,
Rule 2 self-rescued); 89.2% of listings are multi-SKU; `ebay_listings.status` is 99.4% NULL and
self-contradictory (never use it); `performance_data` is a 60-day rolling window. 12 of 14 input columns
fully sourced from the raw DB.

## Duplicate risks
None. First eBay PPC pause-recommendation engine in the portfolio.

## Unresolved gaps (why this is AMBER, not closed)
1. **No technical/queryability reviewer sign-off recorded** (Sajeesan, Tamil Selvan). Business validator
   (Meshika) verified the dashboard, but per the fleet precedent that alone does not close the task.
2. **D02 scheduled-task registration state to confirm.** The TASK_REGISTER D02 row reads *"BUILT — awaiting
   registration"*, while the same-day fleet-alignment log shows it running through the shared engine and a
   live `ph_task` refresh (405→407). Confirm whether `EPPA_Weekly_Pause_Report` is registered and firing
   unattended.

## Decision
**AMBER — D01 delivered, live-verified and business-verified (Meshika); D02 automation built; reviewer
gates and D02 registration confirmation pending.**

## One next action
Confirm whether `EPPA_Weekly_Pause_Report` is registered and running unattended, and obtain the
technical/queryability reviewer sign-offs; then upgrade this record to a full close.

## Git
`04b6ed0`.
